<section class="k-container" style="padding: 48px 40px 60px; max-width: 800px">
  <h1 class="k-h1">{{ page.title }}</h1>
  <div style="font: 500 15px/1.75 var(--k-font-ui); color: var(--k-text-soft)">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Contenu de page", "presets": [{ "name": "Contenu de page" }] }
{% endschema %}
